<%= simple_form_for(@customer) do |f| %>
  
  
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
